## 1. 設計・スキーマ更新

- [x] 1.1 `UserProfile` に在室ステータス用の enum フィールドを追加し、Prisma スキーマとマイグレーションを作成する
- [x] 1.2 既存の `createAttendance` / `updateAttendance` ロジックを分析し、ステータスとの整合性を設計に反映する

## 2. API 実装

- [x] 2.1 `POST /api/attendance/inside-area` エンドポイントを新規作成し、認証済みユーザーのステータスを「学内」に更新する
- [x] 2.2 既存の入室・退室 API を更新し、新しいステータスを設定／リセットする
- [x] 2.3 プロフィール取得系 API (`/api/profiles/*`) でステータス情報を返却する

## 3. フロントエンド更新

- [x] 3.1 ダッシュボードの在室 UI（プロフィール・一覧）にステータス表示を追加する
- [x] 3.2 学内ステータスに切り替える UI（ボタンやメニュー）を追加し、API 呼び出しと連携させる
- [x] 3.3 既存の在室表示やスタイルを新ステータスに対応させる

## 4. バリデーション・テスト

- [x] 4.1 Prisma の生成とマイグレーションをローカルで確認する
- [x] 4.2 `npm run lint` を実行し、静的解析エラーがないことを確認する
- [ ] 4.3 手動で主要フロー（入室・退室・学内切替・UI 表示）を確認する
