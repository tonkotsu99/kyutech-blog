## 1. API 更新

- [ ] 1.1 `/api/attendance/all` が `presenceStatus` を返すように Prisma クエリを更新する
- [ ] 1.2 Localabo 画面で利用する他のデータ取得（必要なら）を確認し、`presenceStatus` を含める

## 2. UI 改修

- [ ] 2.1 `AllAttendanceList` を在室 / 学内 / 学外の 3 カテゴリに分類するロジックへ変更する
- [ ] 2.2 学内カテゴリー向けのカード UI・バッジ・カウンター表示を追加し、既存リンク操作を保つ

## 3. バリデーション

- [ ] 3.1 `npm run lint` を実行して静的解析エラーがないことを確認する
- [ ] 3.2 ブラウザで Localabo 画面を開き、在室・学内・学外ステータスが期待通りに表示されることを手動確認する
